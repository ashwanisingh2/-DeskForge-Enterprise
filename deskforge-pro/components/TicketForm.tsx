'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';let subs:any={Hardware:['Laptop','Desktop','Monitor','Keyboard/Mouse','Headset'],Software:['Microsoft Office','Antivirus','ERP/SAP','Custom App','OS'],Network:['WiFi','VPN','Internet','LAN/Ethernet','Firewall'],'Access & Permissions':['New Account','Password Reset','Permission Change','Account Locked'],Email:['Outlook Setup','Email not sending','Spam','Distribution List'],Printer:['Offline','Paper Jam','Driver Issue','Network Printer']};export function TicketForm(){let[d,setD]=useState<any>({title:'',description:'',category:'Hardware',subcategory:'Laptop',priority:'MEDIUM',impact:'Individual',urgency:'Medium',source:'Web Portal',tags:[]}),[busy,setBusy]=useState(false),[error,setError]=useState(''),r=useRouter();function set(k:string,v:any){setD({...d,[k]:v})}async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError('');let x=await fetch('/api/tickets',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(d)}),j=await x.json();setBusy(false);if(!x.ok)setError(j.error||'Could not create ticket');else r.push('/tickets/'+j.id)}return <form onSubmit={submit} className="grid gap-5 lg:grid-cols-3"><section className="card lg:col-span-2"><label className="font-semibold">Title</label><input className="input mb-5 mt-2" required maxLength={150} value={d.title} onChange={e=>set('title',e.target.value)}/><label className="font-semibold">Description</label><textarea className="input mt-2 min-h-52" required value={d.description} onChange={e=>set('description',e.target.value)}/><label className="mt-5 block font-semibold">Tags</label><input className="input mt-2" placeholder="vpn, remote" onChange={e=>set('tags',e.target.value.split(',').map(x=>x.trim()).filter(Boolean))}/></section><aside className="card"><label className="font-semibold">Priority</label><select className="input mb-4 mt-2" value={d.priority} onChange={e=>set('priority',e.target.value)}>{['LOW','MEDIUM','HIGH','CRITICAL'].map(x=><option key={x}>{x}</option>)}</select><label className="font-semibold">Category</label><select className="input mb-4 mt-2" value={d.category} onChange={e=>setD({...d,category:e.target.value,subcategory:subs[e.target.value]?.[0]})}>{Object.keys(subs).map(x=><option key={x}>{x}</option>)}</select><label className="font-semibold">Subcategory</label><select className="input mb-4 mt-2" value={d.subcategory} onChange={e=>set('subcategory',e.target.value)}>{(subs[d.category]||[]).map((x:string)=><option key={x}>{x}</option>)}</select><label className="font-semibold">Impact</label><select className="input mb-4 mt-2" value={d.impact} onChange={e=>set('impact',e.target.value)}><option>Individual</option><option>Department</option><option>Company-wide</option></select>{error&&<p className="mb-3 text-red-600">{error}</p>}<button disabled={busy} className="btn w-full">{busy?'Creating…':'Create Ticket'}</button></aside></form>}
