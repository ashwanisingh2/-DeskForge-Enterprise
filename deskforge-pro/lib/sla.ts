import type {Priority,Ticket} from '@prisma/client';const rules={CRITICAL:{responseHrs:1,resolutionHrs:4},HIGH:{responseHrs:4,resolutionHrs:8},MEDIUM:{responseHrs:8,resolutionHrs:24},LOW:{responseHrs:24,resolutionHrs:72}} as const;export function getSLAConfig(p:Priority){return rules[p]}export function calculateDueDate(p:Priority,createdAt=new Date()){return new Date(createdAt.getTime()+rules[p].resolutionHrs*3600000)}export function getSLAStatus(t:Pick<Ticket,'dueDate'|'status'|'createdAt'>){if(!t.dueDate||['RESOLVED','CLOSED'].includes(t.status))return'ON_TRACK';let total=t.dueDate.getTime()-t.createdAt.getTime(),left=t.dueDate.getTime()-Date.now();return left<=0?'BREACHED':left/total<.2?'AT_RISK':'ON_TRACK'}export function getTimeRemaining(d:Date){let n=d.getTime()-Date.now(),a=Math.abs(n);return{hours:Math.floor(a/3600000),minutes:Math.floor(a%3600000/60000),isBreached:n<0}}
