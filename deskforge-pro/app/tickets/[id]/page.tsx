import {notFound} from 'next/navigation';import {prisma} from '@/lib/prisma';import {Shell} from '@/components/Shell';import {TicketDetail} from '@/components/TicketDetail';export default async function Page({params}:{params:{id:string}}){let t=await prisma.ticket.findFirst({where:{id:params.id,deletedAt:null},include:{requester:true,assignee:true,comments:{include:{author:true},orderBy:{createdAt:'asc'}},activityLogs:{include:{user:true}}}});if(!t)notFound();return <Shell><TicketDetail initial={t}/></Shell>}
