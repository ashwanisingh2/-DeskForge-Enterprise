import {prisma} from '@/lib/prisma';import {Shell} from '@/components/Shell';import {requireUser} from '@/lib/session';export const dynamic='force-dynamic';export default async function Page(){let session=await requireUser('user:manage'),u=await prisma.user.findMany({where:{tenantId:session.tenantId},orderBy:{name:'asc'}});return <Shell><div className="mb-6 flex justify-between"><div><h1 className="text-3xl font-bold">User Management</h1><p className="text-slate-500">Access, roles and service ownership.</p></div><button className="btn">+ Add User</button></div><div className="card overflow-x-auto p-0"><table className="table"><thead><tr><th>Name</th><th>Email</th><th>Role</th><th>Department</th><th>Status</th></tr></thead><tbody>{u.map(x=><tr key={x.id}><td className="font-semibold">{x.name}</td><td>{x.email}</td><td>{x.role}</td><td>{x.department||'—'}</td><td>{x.isActive?'🟢 Active':'⚪ Inactive'}</td></tr>)}</tbody></table></div></Shell>}

