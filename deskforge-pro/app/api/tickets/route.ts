import {NextRequest,NextResponse} from 'next/server';import {prisma} from '@/lib/prisma';import {requireUser} from '@/lib/session';import {ticketSchema} from '@/lib/validations';import {calculateDueDate} from '@/lib/sla';import {sendTicketAssignedEmail,sendTicketCreatedEmail} from '@/lib/email';export async function GET(req:NextRequest){try{let u=await requireUser(),q=req.nextUrl.searchParams,page=Math.max(1,+(q.get('page')||1)),limit=Math.min(100,+(q.get('limit')||25)),where:any={deletedAt:null};if(u.role==='END_USER')where.requesterId=u.id;for(let k of ['status','priority','category','assigneeId','requesterId','slaStatus'])if(q.get(k))where[k]=q.get(k)!.toUpperCase().replace('-','_');if(q.get('search'))where.OR=['id','title','description'].map(k=>({[k]:{contains:q.get('search'),mode:'insensitive'}}));if(q.get('dateFrom')||q.get('dateTo'))where.createdAt={...(q.get('dateFrom')&&{gte:new Date(q.get('dateFrom')!)}),...(q.get('dateTo')&&{lte:new Date(q.get('dateTo')!)})};let[tickets,total]=await prisma.$transaction([prisma.ticket.findMany({where,include:{assignee:true,requester:true},skip:(page-1)*limit,take:limit,orderBy:{[q.get('sortBy')||'createdAt']:q.get('sortOrder')==='asc'?'asc':'desc'}}),prisma.ticket.count({where})]);return NextResponse.json({tickets,total,page,totalPages:Math.ceil(total/limit)})}catch(e){return NextResponse.json({error:'Unauthorized'},{status:401})}}export async function POST(req:NextRequest){try{let u=await requireUser(),data=ticketSchema.parse(await req.json()),last=await prisma.ticket.findFirst({orderBy:{createdAt:'desc'},select:{id:true}}),id=`TKT-${String((+(last?.id.split('-')[1]||0))+1).padStart(4,'0')}`,ticket=await prisma.ticket.create({data:{...data,id,requesterId:u.id,dueDate:calculateDueDate(data.priority),activityLogs:{create:{action:'created',detail:'Ticket created',userId:u.id}}},include:{requester:true,assignee:true}});await prisma.auditLog.create({data:{userId:u.id,action:'CREATE',entityType:'Ticket',entityId:id,newValue:ticket as any}});await sendTicketCreatedEmail(ticket,ticket.requester);if(ticket.assignee)await sendTicketAssignedEmail(ticket,ticket.assignee);return NextResponse.json(ticket,{status:201})}catch(e:any){return NextResponse.json({error:e.message},{status:e.message==='UNAUTHORIZED'?401:400})}}
