import {Prisma} from '@prisma/client';import {NextRequest,NextResponse} from 'next/server';import {z} from 'zod';import {prisma} from '@/lib/prisma';import {requireUser} from '@/lib/session';const schema=z.object({name:z.string().min(3),description:z.string().min(10),category:z.string().min(2),formSchema:z.record(z.string(),z.unknown()),approvalSchema:z.record(z.string(),z.unknown()).nullable().optional(),fulfillmentTeam:z.string().optional(),deliveryHours:z.number().int().positive().optional(),cost:z.number().nonnegative().nullable().optional()});export async function GET(){let u=await requireUser(),items=await prisma.serviceCatalogItem.findMany({where:{tenantId:u.tenantId,isActive:true},orderBy:[{category:'asc'},{name:'asc'}]});return NextResponse.json({items,total:items.length})}export async function POST(req:NextRequest){try{let u=await requireUser('catalog:manage'),data=schema.parse(await req.json()),item=await prisma.serviceCatalogItem.create({data:{...data,formSchema:data.formSchema as Prisma.InputJsonObject,approvalSchema:data.approvalSchema as Prisma.InputJsonObject|undefined,tenantId:u.tenantId}});await prisma.auditLog.create({data:{tenantId:u.tenantId,userId:u.id,action:'CREATE',entityType:'CatalogItem',entityId:item.id,newValue:{name:item.name,category:item.category}}});return NextResponse.json(item,{status:201})}catch(e){let m=e instanceof Error?e.message:'INVALID_REQUEST';return NextResponse.json({error:m},{status:m==='FORBIDDEN'?403:400})}}
