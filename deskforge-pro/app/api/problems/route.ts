import {NextRequest,NextResponse} from 'next/server';import {z} from 'zod';import {prisma} from '@/lib/prisma';import {requireUser} from '@/lib/session';const schema=z.object({title:z.string().min(4),description:z.string().min(10),ownerId:z.string(),rootCause:z.string().optional(),workaround:z.string().optional(),ticketIds:z.array(z.string()).default([])});export async function GET(){let u=await requireUser('problem:manage'),problems=await prisma.problem.findMany({where:{tenantId:u.tenantId},include:{owner:{select:{name:true}},_count:{select:{incidents:true}}},orderBy:{updatedAt:'desc'}});return NextResponse.json({problems,total:problems.length})}export async function POST(req:NextRequest){try{let u=await requireUser('problem:manage'),{ticketIds,...data}=schema.parse(await req.json()),valid=await prisma.ticket.findMany({where:{tenantId:u.tenantId,id:{in:ticketIds}},select:{id:true}}),count=await prisma.problem.count({where:{tenantId:u.tenantId}}),id=`PRB-${String(count+1).padStart(4,'0')}`,problem=await prisma.$transaction(async tx=>{let created=await tx.problem.create({data:{...data,id,tenantId:u.tenantId,incidents:{create:valid.map(t=>({ticketId:t.id}))}}});await tx.auditLog.create({data:{tenantId:u.tenantId,userId:u.id,action:'CREATE',entityType:'Problem',entityId:id,newValue:{title:data.title,linkedIncidents:valid.length}}});return created});return NextResponse.json(problem,{status:201})}catch(e){let m=e instanceof Error?e.message:'INVALID_REQUEST';return NextResponse.json({error:m},{status:m==='FORBIDDEN'?403:400})}}
