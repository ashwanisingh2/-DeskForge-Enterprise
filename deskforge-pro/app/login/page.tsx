'use client';import {useState} from 'react';import {signIn} from 'next-auth/react';import {useRouter} from 'next/navigation';export default function Login(){let[u,setU]=useState('admin'),[p,setP]=useState('admin123'),[show,setShow]=useState(false),[error,setError]=useState(''),[busy,setBusy]=useState(false),r=useRouter();async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);let x=await signIn('credentials',{username:u,password:p,redirect:false});setBusy(false);if(x?.error)setError('Invalid username or password');else r.push('/dashboard')}return <main className="grid min-h-screen md:grid-cols-2"><section className="hidden bg-gradient-to-br from-slate-950 via-blue-950 to-blue-600 p-20 text-white md:flex md:flex-col md:justify-center"><h1 className="text-5xl font-extrabold">Support, forged for momentum.</h1><p className="mt-5 max-w-xl text-lg text-blue-100">Enterprise incidents, requests, knowledge and service intelligence in one calm workspace.</p></section><section className="grid place-items-center p-8"><form onSubmit={submit} className="w-full max-w-md"><h1 className="text-3xl font-extrabold">◆ DeskForge Pro</h1><p className="mb-8 mt-2 text-slate-500">Sign in to your service workspace.</p><label className="font-semibold">Username</label><input className="input mb-5 mt-2" value={u} onChange={e=>setU(e.target.value)}/><label className="font-semibold">Password</label><div className="relative mt-2"><input className="input" type={show?'text':'password'} value={p} onChange={e=>setP(e.target.value)}/><button type="button" onClick={()=>setShow(!show)} className="absolute right-3 top-2">◉</button></div><label className="my-4 flex gap-2"><input type="checkbox"/> Remember me</label>{error&&<p className="mb-3 text-red-600">{error}</p>}<button disabled={busy} className="btn w-full">{busy?'Signing in…':'Sign in securely'}</button><div className="mt-6 rounded-lg bg-slate-100 p-3 text-sm">Demo: admin / admin123</div></form></section></main>}
