import {beforeEach,describe,expect,it} from 'vitest';import {clearLoginAttempts,consumeLoginAttempt} from '../lib/rate-limit';describe('login rate limiting',()=>{beforeEach(async()=>{delete process.env.REDIS_URL;await clearLoginAttempts('test')});it('allows the first five attempts and blocks the sixth',async()=>{for(let i=0;i<5;i++)expect((await consumeLoginAttempt('test')).allowed).toBe(true);expect((await consumeLoginAttempt('test')).allowed).toBe(false)});it('clears counters after successful authentication',async()=>{for(let i=0;i<5;i++)await consumeLoginAttempt('test');await clearLoginAttempts('test');expect((await consumeLoginAttempt('test')).remaining).toBe(4)})});
